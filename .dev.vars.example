# Copy to .dev.vars (gitignored) for local `wrangler pages dev`.
# Secrets — set the real values here for local, and via
# `wrangler pages secret put NAME` for production.
TMDB_TOKEN=""
OMDB_KEY=""
ANTHROPIC_API_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_PUBLIC_KEY=""
